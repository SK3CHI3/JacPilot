/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  // In Tailwind v4, theme is defined in CSS using @theme
  // This config file is kept for content paths only
}
